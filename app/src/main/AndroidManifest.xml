<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thuctaptotnghiep">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThucTapTotNghiep"
        android:usesCleartextTraffic="true">
        <activity android:name=".NhanVien.ThemSanPhamNVActivity"></activity>
        <activity android:name=".NhanVien.ThemThanhVienNVActivity" />
        <activity android:name=".NhanVien.NhanVienActivity" />
        <activity android:name=".Admin.ThemSanPhamActivity" />
        <activity android:name=".Admin.ThemThanhVienActivity" />
        <activity android:name=".Admin.AdminActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>